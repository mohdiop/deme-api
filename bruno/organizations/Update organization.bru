meta {
  name: Update organization
  type: http
  seq: 2
}

patch {
  url: {{organizationUrl}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Kodonso"
  }
}

settings {
  encodeUrl: true
}
